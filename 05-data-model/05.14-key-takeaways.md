# Chapter 05 — Key Takeaways

This chapter had code — all the schema definitions are committed to the repo. But the concepts behind the decisions are what the viva will ask about.

Before moving to chapter 06, you should be able to explain:

- [ ] Why passwordHash uses select: false — what it prevents and how to explicitly include it when needed
- [ ] Why amountPaid on Enrollment is a price snapshot, not a cache — and what breaks without it
- [ ] Why the Enrollment model has a compound unique index on { studentId, courseId }
- [ ] What the TTL index on RefreshToken.expiresAt does — and how MongoDB handles it
- [ ] Why all ten models use references (ObjectId fields) rather than embedding
- [ ] What the pre('save') hook on User does — and what mistake it prevents in route handlers

**Commit your work:**

```
git add server/src/models/
git commit -m "feat(models): all Mongoose schemas defined, indexes, seed script"
```

**Write your answers** in learning-log/05-data-model.md — the why behind each schema decision.

All complete? Then continue to chapter 06 — Authentication API.
