# Chapter 19 — Definition of Done

- [ ] GET /api/admin/courses/pending returns courses with status 'pending'
- [ ] PATCH /api/admin/courses/:id/approve changes status to 'published' and invalidates the cache
- [ ] PATCH /api/admin/courses/:id/reject stores rejectionReason and changes status to 'rejected'
- [ ] GET /api/admin/metrics returns totalUsers, publishedCourses, totalEnrollments, totalRevenue
- [ ] Non-admin token on /api/admin/* returns 403
- [ ] Admin frontend shows pending queue and metrics
- [ ] git commit: "feat(admin): approve/reject courses, metrics, user management"

All boxes ticked? Continue to chapter 20 — Deploy.
