# Chapter 02 — Definition of Done

Tick every box before moving to chapter 03. Do not advance with an unticked box.

- [ ] server/ directory exists with src/app.js, src/server.js, src/config/index.js, src/lib/db.js, and src/middleware/ folder
- [ ] client/ directory exists with app/, components/, lib/, store/, and types/ folders
- [ ] npm run dev in server/ starts without errors; console prints the port message
- [ ] npm run dev --port 3001 in client/ starts without errors; Next.js opens at http://localhost:3001
- [ ] GET http://localhost:3000/api/health returns { "status": "ok", "timestamp": "..." }
- [ ] The Next.js homepage fetches from /api/health and displays "API status: ok"
- [ ] No CORS errors appear in the browser DevTools console
- [ ] server/.env is created with DATABASE_URL, JWT_SECRET, JWT_REFRESH_SECRET, FRONTEND_URL
- [ ] client/.env.local is created with NEXT_PUBLIC_API_URL
- [ ] Neither .env file appears in git status or git log
- [ ] docker compose up -d starts MongoDB and Redis; docker compose ps shows both as running
- [ ] git log shows a clean skeleton commit

All boxes ticked? Then continue to chapter 03 — Why MongoDB? If not, that is where today's work is.
