# Test the Health Check

Before moving on, verify everything you have built so far from the command line.

**Backend:**

```
curl http://localhost:3000/api/health
```

Expected:
```json
{ "status": "ok", "timestamp": "2026-02-15T10:00:00.000Z" }
```

**Frontend (in browser):** http://localhost:3001 shows "API status: ok".

**CORS check:** Open browser DevTools > Network tab. Reload http://localhost:3001. The /api/health request should return 200 with no CORS errors in the Console tab.

**Missing .env check:**

```
cd server
unset DATABASE_URL   # temporarily remove from environment
npm run dev
```

For now, this may not fail fast (config validation comes in chapter 04). After that chapter, the server should refuse to start with a clear error listing every missing variable.

**Commit your progress:**

```
git add -A
git commit -m "feat: project skeleton — both apps boot, health check works"
```

✅ All checks above pass.
✅ git log shows a clean skeleton commit.
❌ Do not proceed to chapter 03 with a broken health check.
