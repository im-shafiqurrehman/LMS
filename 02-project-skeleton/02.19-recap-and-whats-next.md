# Recap and What Is Next

Here is what you have built in this chapter:

- A monorepo with server/ and client/ as sibling applications
- An Express.js backend that starts, applies security headers, and serves GET /api/health
- A Next.js frontend that starts and fetches from the backend
- Git-safe environment variables in server/.env and client/.env.local
- Local MongoDB and Redis services via Docker Compose

What you have NOT done yet:
- Config validation at startup (chapter 04)
- Mongoose connection (chapter 04)
- Any authentication (chapter 06)
- Any real features (chapters 09+)

**What is next:** Chapter 03 is a concept chapter — "Why MongoDB?" — that explains the database and ODM choices before you connect to them. It is shorter than this chapter and has no code to write. Read it, write your learning log entry, and then chapter 04 wires the actual database connection.

**Before you move on — checklist gate below.**
