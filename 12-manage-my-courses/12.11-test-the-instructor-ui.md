# Test the Instructor UI

**Test 1 — Access control:**
1. Log in as a student; try to visit /dashboard/instructor
2. Expected: redirect to /dashboard (student role blocked by RoleGuard)

**Test 2 — Course creation:**
1. Log in as an instructor
2. Navigate to /dashboard/instructor
3. Click "Create Course" and fill in the form
4. Expected: redirected to the course edit page

**Test 3 — Lesson management:**
1. On the edit page, add 3 lessons with different durations
2. Expected: lesson list updates; totalLessons shows 3 in the API response

**Test 4 — Submit for review:**
1. With 3 lessons, click "Submit for Review"
2. Expected: course status badge changes to PENDING; edit controls become read-only

**Test 5 — Git commit:**
After all tests pass:
```
git add client/
git commit -m "feat(instructor-ui): course dashboard, create, edit, lessons, submit"
```