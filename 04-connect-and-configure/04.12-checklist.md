# Chapter 04 — Definition of Done

- [ ] src/config/index.js reads all required env variables through a Zod schema
- [ ] The server refuses to start (process.exit(1)) if DATABASE_URL is missing or empty — and prints the exact variable name
- [ ] src/lib/db.js exports a connectDB() function; server.js calls it before app.listen()
- [ ] The server console prints "MongoDB connected: localhost" followed by the port message on startup
- [ ] src/middleware/errorHandler.js is the last middleware mounted in app.js
- [ ] A thrown AppError('message', 404) returns HTTP 404 { "error": "message" }
- [ ] src/middleware/validate.js exports a validate(schema) middleware factory
- [ ] GET /api/health still returns { "status": "ok" }
- [ ] No .env file or real secret appears in git status or git log

All boxes ticked? Then continue to chapter 05 — Data Model. If not, that is where today's work is.
