# Learning Log Prompt

Write your chapter 09 entry in learning-log/09-course-catalogue.md.

Answer the following (in your own words, not copied from this guide):

1. What is the N+1 query problem? Give an example using EduFlow's data.
2. Why does offset pagination degrade at high page numbers? What does cursor pagination use instead?
3. What does an IXSCAN vs COLLSCAN tell you in a MongoDB explain plan?
4. Why does the catalogue API return lesson titles and durations but not video URLs?

Each answer should be 3–5 sentences. If you cannot write 3 sentences about it, you need to re-read the sub-chapter.

Commit your learning log:
```
git add learning-log/
git commit -m "docs(learning-log): chapter 09 catalogue notes"
```
