# Recap and What Is Next

This chapter built the complete payment and enrollment backend:

- PaymentIntent creation (with studentId and courseId in metadata)
- Stripe webhook handler with signature verification
- Idempotent enrollment confirmation
- List enrollments and check access endpoints

What is next: Chapter 14 — Enroll in a Course (Frontend). The Stripe Elements checkout form, payment result handling, and the student's My Learning page.

**Commit:**
```
git add server/src/modules/enrollments/
git commit -m "feat(enrollments): Stripe payment intent, webhook, idempotent enrollment"
```
