# Learning Log Prompt

Write learning-log/13-enrollments.md before moving on.

Answer in your own words:

1. Why does the enrollment happen in the webhook handler, not in the payment intent response?
2. What does "idempotent" mean? Give a concrete example from the enrollment flow.
3. What is the price snapshot problem, and how does amountPaid solve it?
4. Why does the webhook handler need the raw (unparsed) request body?

Commit: git add learning-log/ && git commit -m "docs(learning-log): chapter 13 enrollment notes"
