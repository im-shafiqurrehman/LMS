# Apply It to Your Repo

This is a concept chapter — but concept chapters still move the project. Here is your applied work:

**1. Open server/src/models/ and create placeholder files** for each model you will build in chapter 05:

```
touch server/src/models/user.model.js
touch server/src/models/course.model.js
touch server/src/models/category.model.js
touch server/src/models/lesson.model.js
touch server/src/models/enrollment.model.js
touch server/src/models/progressRecord.model.js
touch server/src/models/certificate.model.js
touch server/src/models/question.model.js
touch server/src/models/answer.model.js
touch server/src/models/refreshToken.model.js
touch server/src/models/index.js
```

**2. In a text editor, sketch the relationship between User and Enrollment** in plain English:

> "An Enrollment has studentId (references User) and courseId (references Course). It is referenced by ProgressRecord and Certificate. When a student pays, we snapshot amountPaid onto the Enrollment so the price is frozen."

Write this in learning-log/03-why-mongodb.md.

**3. Think through one embed vs reference decision in your own words** — could lessons be embedded in courses? Why or why not? Write it down.

Committing placeholder model files now means chapter 05 starts with a clean foundation, not creating files from scratch.

```
git add server/src/models/
git commit -m "chore(models): placeholder model files scaffolded"
```
