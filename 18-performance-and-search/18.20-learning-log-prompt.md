# Learning Log Prompt

Write learning-log/18-performance.md:

1. What is the cache-aside pattern and why does it require careful invalidation?
2. Why does SCAN beat KEYS for production Redis key deletion?
3. What does fuzzy search in Atlas Search enable that regex search cannot do?
4. Why is in-memory rate limiting insufficient in a multi-instance deployment?

Commit: git add learning-log/ && git commit -m "docs(learning-log): chapter 18 performance notes"
