# Mandatory Reads

**1. Redis caching fundamentals**
Search "Redis caching patterns Node.js ioredis" — understand get/set/del with TTL, and the cache-aside pattern.

**2. MongoDB Atlas Search tutorial**
Search "MongoDB Atlas Search tutorial Node.js" — the official tutorial walks through index creation and the $search aggregation stage.

**3. Rate limiting best practices**
Search "API rate limiting best practices Node.js" — understand the difference between in-memory and distributed rate limiting (Redis-backed), and why IP-based limiting is insufficient on shared networks.
