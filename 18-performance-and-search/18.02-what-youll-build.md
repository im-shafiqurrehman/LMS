# What You Will Build

1. **Redis caching layer** for GET /api/courses — the catalogue response is cached for 5 minutes and invalidated when a course is published
2. **Atlas Search index** on the Course collection — replaces the regex-based keyword search with a proper full-text search
3. **Rate limiting** on POST /api/auth/login and POST /api/auth/register — prevents brute-force
4. **Notifications stub** — logs email notifications to console (Resend integration as bar-raiser)
