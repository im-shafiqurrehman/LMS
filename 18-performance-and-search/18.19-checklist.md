# Chapter 18 — Definition of Done

- [ ] Redis is connected; GET /api/courses is served from cache on second request
- [ ] Publishing a course invalidates the catalogue cache
- [ ] GET /api/courses?q=keyword uses Atlas Search index (fuzzy matching works)
- [ ] POST /api/auth/login returns 429 after 10 requests within 15 minutes from the same IP
- [ ] git commit: "feat(scale): Redis catalogue cache, Atlas Search, auth rate limiting"

All boxes ticked? Continue to chapter 19 — Admin Dashboard.
