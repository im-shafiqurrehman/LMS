# Recap and What Is Next

This chapter added:

- store/courseSlice.ts — manages catalogue state in Redux
- /courses page with course grid, category filter, keyword search, and cursor-based Load More
- /courses/[id] course detail page with lesson list
- CategoryFilter and LessonList components
- GET /api/courses/categories endpoint on the server

What is next: Chapter 11 — Instructor Courses API. Instructors need to create courses, add lessons, upload cover images, and submit for admin review. This is the write side of the course management feature.

**Commit:**
```
git add client/
git commit -m "feat(catalogue-ui): course grid, filter, search, load more, detail page"
```
