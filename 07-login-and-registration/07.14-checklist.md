# Chapter 07 — Definition of Done

- [ ] store/authSlice.ts implements setCredentials and logout actions
- [ ] lib/api.ts is the single axios instance; request interceptor adds Authorization header; response interceptor dispatches logout on 401
- [ ] app/register/page.tsx — form works, navigates to /verify on success
- [ ] app/verify/page.tsx — OTP form works, navigates to /login on success
- [ ] app/login/page.tsx — form works, dispatches setCredentials, navigates to /dashboard
- [ ] Providers.tsx rehydrates auth state using the refresh token from sessionStorage on page load
- [ ] AuthGuard component redirects unauthenticated users to /login
- [ ] Navbar shows email + Logout for logged-in users, Login + Register for guests
- [ ] Full flow tested in browser: register → verify → login → dashboard → refresh → logout
- [ ] git commit with: "feat(auth-ui): register, verify, login, logout, auth guard"

All boxes ticked? Then continue to chapter 08 — Authorization.
